{
  "$comment": "Base manifest. The origin-derived arrays — content_scripts[].matches and host_permissions — are filled in at build time from packages/config/origins.json, so the sites this extension may touch are declared in exactly one place. See build.mjs.",
  "manifest_version": 3,
  "name": "Liha WebMCP Adapter",
  "version": "1.0.0",
  "description": "Make any website agent-ready: register WebMCP tools on sites that never implemented WebMCP.",
  "minimum_chrome_version": "111",
  "background": {
    "service_worker": "service-worker.js",
    "type": "module"
  },
  "action": {
    "default_popup": "popup/popup.html",
    "default_title": "Liha WebMCP Adapter"
  },
  "permissions": ["scripting", "storage"],
  "optional_host_permissions": ["http://*/*", "https://*/*"],
  "content_security_policy": {
    "extension_pages": "script-src 'self'; object-src 'self'"
  }
}
